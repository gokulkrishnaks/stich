#ifndef STICH_RUNTIME_EVENTS_COMMAND_EVENT_H
#define STICH_RUNTIME_EVENTS_COMMAND_EVENT_H

/* Command events currently share the canonical event definition in
 * `event_types.h`. This wrapper keeps room for command-specific helpers later. */
#include "runtime/events/event_types.h"

#endif
