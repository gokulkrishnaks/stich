#ifndef STICH_RUNTIME_EVENTS_TELEMETRY_EVENT_H
#define STICH_RUNTIME_EVENTS_TELEMETRY_EVENT_H

/* Telemetry events currently share the canonical event definition in
 * `event_types.h`. This wrapper keeps room for telemetry-specific helpers later. */
#include "runtime/events/event_types.h"

#endif
