#ifndef STICH_RUNTIME_EVENTS_NODE_EVENT_H
#define STICH_RUNTIME_EVENTS_NODE_EVENT_H

/* Node events currently share the canonical event definition in
 * `event_types.h`. This wrapper keeps room for node-specific helpers later. */
#include "runtime/events/event_types.h"

#endif
