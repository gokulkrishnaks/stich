#ifndef STICH_RUNTIME_CORE_RUNTIME_H
#define STICH_RUNTIME_CORE_RUNTIME_H

/* Entry point for bootstrapping the STICH runtime from configuration through
 * adapter registration and the first event loop execution. */
int stich_runtime_start(void);

#endif
